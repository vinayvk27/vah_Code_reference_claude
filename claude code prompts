TASK: Generate a complete API handoff document for our React frontend developer. The document must describe the authentication flow EXACTLY as currently implemented in the codebase — not how it was specified.

REPORT FORMAT:

## 1. Auth Mode Behavior
- What environment variable controls auth mode
- What values it accepts and what each value does
- Default value if env var is absent
- Where in the code this is read

## 2. Login Endpoint — Exact Current Behavior
- Full URL path
- HTTP method
- Required headers
- Exact request body schema (field names, types, required vs optional)
- Show 2 example request bodies — one for prod mode, one for dev mode
- Exact success response body (field names, types, including any nested 
  objects like user_profile — show the FULL shape)
- All possible error responses with their HTTP codes and exact 
  "detail" message strings the user might see

## 3. JWT Payload Shape
List every field in the JWT payload (decode and inspect a sample if 
needed). Mark which fields are critical for the frontend to read.

## 4. localStorage Contract
- Which keys must the frontend store after login
- What is the value of each key
- When should each key be cleared (on logout, 401, etc.)

## 5. Test Credentials (Dev Mode)
- Run a query on the resources table to get the dept values for 
  emp_no 3428, 3010, 3117
- Show the actual emp_no/dept pairs the React dev should use to test

## 6. What Other Endpoints Changed
- List every endpoint that was modified in this auth-bypass change
- For each, note if request/response shape changed or stayed the same

## 7. Anything the React Dev Should Know
- Edge cases discovered during implementation
- Caveats (e.g., "dept comparison is case-insensitive on the backend")
- Anything unusual or different from a textbook auth flow

Format the entire report as plain markdown text. The output will be 
copy-pasted into a chat message and sent to the React developer.

DO NOT:
- Document features not actually present in the code
- Speculate about future changes
- Include any information about the LDAP code path beyond "still 
  works as before, no contract changes there"
