try:
        response = llm.with_structured_output(RMSIntentClassification).invoke(prompt)
        print(f"[DIAG-CLASSIFIER] LLM returned: {response}")
        return response
    except Exception as e:
        import traceback
        print(f"[DIAG-CLASSIFIER] LLM call failed: {type(e).__name__}: {e}")
        print(f"[DIAG-CLASSIFIER] Traceback:")
        traceback.print_exc()
        return keyword_fallback(user_message)