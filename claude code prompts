All done. Summary of what was done:

Deleted:

app/routes/agent_route.py — agent capabilities routes with collection_agents references
Modified:

main.py — removed commented-out agent_route import and router include (2 lines)
ARCHITECTURE_DECISIONS.md — appended Decision 15
Created:

TASK_3_5_REPORT.md — full report with all grep results
Grep results:

collection_agents: 24 matches, all in out-of-scope files (workflows/, chat_controller.py) or comments — 0 deletions needed
allowed_agents: 0 matches
RoleCollectionAccess: 0 matches
role_collection_access: 0 matches
Key finding from Action 4 scan: Line 137 in chat_controller.py has a bug — user.get("collection_agents") returns None with the new CurrentUser, causing form submission permission checks to always fail. Logged for Task 4.
