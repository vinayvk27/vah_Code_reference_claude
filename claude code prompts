═══════════════════════════════════════════════════════════════════
TASK 5.8: Guard against broken form_flow path in MRR creator
═══════════════════════════════════════════════════════════════════
When user types a create-MRR phrase without uploading an Excel file,
return a friendly message asking for the file. Do NOT route to the
broken form_flow path that emits malformed widget JSON.

───────────────────────────────────────────────────────────────────
CRITICAL CONSTRAINT
───────────────────────────────────────────────────────────────────
- Only touch app/workflows/material_workflow/nodes.py.
- Modify ONLY creator_path_router (or the function that decides
  whether to route to excel_handler vs form_flow).
- Do NOT modify the form_flow code itself — just bypass it.
- Do NOT modify any other file.

───────────────────────────────────────────────────────────────────
SCOPE
───────────────────────────────────────────────────────────────────

In creator_path_router (look for the routing logic that decides
"excel_handler" vs "form_flow"):

Currently it routes:
- If uploaded_files present → excel_handler
- Else → form_flow (BROKEN)

Change to:
- If uploaded_files present → excel_handler
- Else → return a message "Please upload an Excel file with the MRR
  details. The Excel format should include: Project Number,
  Requisition Type, Need By Date, Description, Comments, Item Name,
  Requested Quantity, Return Date." and end the flow.

The cleanest way: add a new terminal node "creator_needs_file" that
emits this message, and route to it instead of form_flow when no
files are uploaded.

If the routing function returns a string identifying the next node,
add a new conditional case for the no-file scenario.

If easier, just add a node-level guard inside creator_path_router
that returns a message directly when has_pending=False and no files.

───────────────────────────────────────────────────────────────────
SUGGESTED IMPLEMENTATION
───────────────────────────────────────────────────────────────────

In creator_path_router (or wherever it decides routing):

  if uploaded_files:
      return "excel_handler"
  else:
      # form_flow is currently broken; redirect to a helpful message
      return "creator_needs_file"

Then add a new node "creator_needs_file" that emits:

async def creator_needs_file_node(state: MaterialRequisitionState) -> dict:
    msg = (
        "To create a material requisition, please upload an Excel file with these columns: "
        "Project Number, Requisition Type, Need By Date, Description, Comments, "
        "Item Name, Requested Quantity, Return Date.\n\n"
        "Once uploaded, type 'process this mrr' and I'll handle the rest."
    )
    return {"messages": [AIMessage(content=msg)]}

And register it in the graph:
- graph.add_node("creator_needs_file", creator_needs_file_node)
- graph.add_edge("creator_needs_file", END)

This requires graph.py to also be touched. ONLY add the node and
edge — do not modify any other existing nodes or edges.

───────────────────────────────────────────────────────────────────
DELIVERABLE
───────────────────────────────────────────────────────────────────
CREATE TASK_5_8_REPORT.md with:
- Files modified (should be nodes.py and graph.py only)
- The new node function
- The new graph node registration and edge
- Confirmation that form_flow node and its broken code is still
  registered (just not reached for this case) so we don't break
  anything else
═══════════════════════════════════════════════════════════════════
END
═══════════════════════════════════════════════════════════════════