# TASK 5.5 REPORT: Strict Prompt Rewrite for RMS Agent Nodes

## INTENDED
Replace the `system_prompt` string construction in the three persona agent nodes (`creator_agent_node`, `reviewer_agent_node`, `approver_agent_node`) with directive, structured prompts that enforce the `<<ACTION>>`...`<<END>>` protocol and reduce LLM disobedience patterns (missing action emission, hallucinated confirmations, silent responses).

## ACTUAL
All three `system_prompt` blocks were successfully replaced in `app/workflows/rms_workflow/nodes.py`:

### creator_agent_node (line ~461)
- Replaced with identity-based prompt containing 6 explicit rules
- Enforces `<<ACTION>>` emission before any confirmation
- Adds date validation rules and missing-field handling
- Uploaded files section rewritten with explicit column mapping

### reviewer_agent_node (line ~735)
- Replaced with identity-based prompt containing 5 explicit rules
- Enforces own-only scope with denial language
- Enforces `<<ACTION>>` emission before any "hours logged" statement
- Missing-field responses enforced

### approver_agent_node (line ~994)
- Replaced with identity-based prompt containing 5 explicit rules
- Enforces `<<ACTION>>` emission before any "Approved"/"Rejected" confirmation
- Rejects "approve all" with specific response
- Requires explicit worklog ID for approvals

## SCOPE CONFIRMATION
- **Only** the `system_prompt` string construction in the three persona nodes was modified
- No function signatures were changed
- No tool bindings were modified
- No action parsing logic was changed
- No return shapes were changed
- No modifications to `intent.py`, controllers, models, or any other file
- Variable assignments above the prompts (e.g., `base_prompt`, `domain_context`) were left intact per constraints (they are now unused dead code but were not removed per the "only touch system_prompt strings" constraint)
