═══════════════════════════════════════════════════════════════════
TASK 2-FIXUP: Verify and complete the Task 2 deliverables
═══════════════════════════════════════════════════════════════════

CONTEXT
The Task 2 report claimed several changes were made, but live testing
shows the old behavior is still active:
  - Login response returns the OLD shape (user_id, username,
    department=null, permissions=[], collection_agents={})
  - The JWT still contains role, allowed_agents, collections,
    collection_agents fields (even if empty)
  - GET /api/v1/auth/me/access returns 404 / "detail not found"

This means either the new code paths were not wired up, or both old
and new code coexist and the old path is winning.

GOAL
Find the gap between the Task 2 report's claims and the actual
running behavior. Fix it. Do not redo Task 2 — instead, diagnose
where the disconnect is.

DIAGNOSTIC STEPS (do all of these and report findings before fixing)

1. Open app/core/security.py:
   - Show the FULL current contents of create_access_token().
   - Confirm whether it includes "role", "allowed_agents",
     "collections", "collection_agents" in the payload it builds.
   - Confirm what fields it actually includes.

2. Open app/routes/auth_route.py:
   - Show the FULL current login endpoint handler.
   - Confirm what shape it returns (what dict it builds and returns).
   - Confirm whether GET /api/v1/auth/me/access is registered as a
     route. If yes, show its handler. If no, confirm absence.

3. Open app/controllers/auth_controller.py:
   - Show the FULL build_user_context_by_empno function.
   - Confirm whether it returns the new shape
     {"user_profile": ..., "access_tree": ...} or the old shape.
   - Show what generate_user_context returns and what calls it.

4. Open app/models/dto_schemas.py:
   - List every Pydantic model class defined in the file (just names).
   - Confirm whether AccessTree, AccessTreeModule, AccessTreeCollection,
     AccessTreeAgent, LoginResponse, CurrentUser exist.

5. Run this one-liner from the project root:
     grep -n "collection_agents\|allowed_agents" app/ -r --include="*.py"
   Report ALL matches. We want to know every file still referencing
   the old fat-JWT field names.

6. Run this:
     grep -n "me/access\|auth/me" app/ -r --include="*.py"
   Confirm the /me/access endpoint is actually wired.

REPORT BACK
Paste the findings as a markdown file TASK_2_FIXUP_DIAGNOSIS.md at
the project root. Do NOT make any code changes yet. We will decide
the fix once we see the diagnostics.

DO NOT TOUCH ANY OTHER FILES.