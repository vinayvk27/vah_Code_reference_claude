# TASK 5.8 REPORT — Guard Against Broken form_flow Path in MRR Creator

## Files Modified

| File | Changes |
|---|---|
| `app/workflows/material_workflow/nodes.py` | Modified `creator_path_router` default branch; added `creator_needs_file_node` |
| `app/workflows/material_workflow/graph.py` | Imported `creator_needs_file_node`; registered node + edge |

**No other files were modified.** The `form_flow` node and its broken code remain registered and unchanged.

---

## The New Node Function

**File:** `app/workflows/material_workflow/nodes.py` (line ~1340)

```python
def creator_needs_file_node(state: MaterialRequisitionState) -> dict:
    """
    Terminal node: emitted when a creator tries to create an MRR
    without uploading an Excel file.  The form_flow path is
    currently broken for this case, so we short-circuit with a
    friendly message instead.
    """
    print(f"[DIAG-MRR] creator_needs_file_node entered.", flush=True)
    msg = (
        "To create a material requisition, please upload an Excel file with these columns: "
        "Project Number, Requisition Type, Need By Date, Description, Comments, "
        "Item Name, Requested Quantity, Return Date.\n\n"
        "Once uploaded, type 'process this mrr' and I'll handle the rest."
    )
    return {
        "messages": [AIMessage(content=msg)],
        "execution_log": state.get("execution_log", []) + [
            "Creator needs file: no uploaded_files; redirected to instructions."
        ]
    }
```

---

## Routing Change in `creator_path_router`

**File:** `app/workflows/material_workflow/nodes.py` (line ~1073)

**Before:**
```python
    # Default — form flow
    print(f"[DIAG-MRR] creator_path_router -> form_flow", flush=True)
    return "form_flow"
```

**After:**
```python
    # No files and no confirm/cancel — ask user to upload Excel
    # (form_flow is currently broken for this case; bypass it)
    print(f"[DIAG-MRR] creator_path_router -> creator_needs_file", flush=True)
    return "creator_needs_file"
```

---

## Graph Registration

**File:** `app/workflows/material_workflow/graph.py`

**Import added (line ~29):**
```python
    creator_needs_file_node,
```

**Node registered (line ~138):**
```python
    # Task 5.8: new terminal node for missing-file case
    graph_builder.add_node("creator_needs_file", creator_needs_file_node)
```

**Conditional edge added to router mapping (line ~173):**
```python
            "creator_needs_file": "creator_needs_file",
```

**Terminal edge added (line ~231):**
```python
    graph_builder.add_edge("creator_needs_file", END)
```

---

## Confirmation: `form_flow` Node Still Registered

The `form_flow` routing key (`"form_flow": "parse_user_input"`) still exists in the conditional edges dict on line ~172 of `graph.py`. The `form_flow` path is simply no longer reached from `creator_path_router` when no files are uploaded — it remains registered for any other code path that might route to it. No `form_flow` node code was modified or removed.

---

## Routing Summary

```
creator_path_router():
  if pending and confirmed  → "submit_mrr"
  if pending and cancelled  → "cancel_mrr"
  if uploaded_files present → "excel_handler"
  else (no files)           → "creator_needs_file"  ← NEW
  (old default)             → "form_flow"           ← still in graph, not reachable here
```
