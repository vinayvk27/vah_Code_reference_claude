═══════════════════════════════════════════════════════════════════
TASK 6.1-FIX2: Add RMS creator to static_output_nodes
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/controllers/chat_controller.py
- Only add node names to the existing static_output_nodes list
- Do NOT change any other logic

CONTEXT
The RMS creator_agent_node is now procedural (no LLM streaming). It
returns its message via the node return value. But the controller
only surfaces non-streaming node output for nodes listed in
static_output_nodes. The RMS creator isn't listed, so its output is
invisible in the UI even though the DB insert succeeds.

There are TWO static_output_nodes lists in chat_controller.py — one
in stream_chat_with_orchestrator and one in stream_chat_with_file.
Update BOTH (or whichever the RMS /stream path uses — to be safe,
update both).

CHANGE
Add these entries to the static_output_nodes list(s):
    "creator_agent",
    "creator_agent_node",

Place them near the existing RMS-related or permission_blocked
entries. Add a comment: "# RMS procedural creator (no LLM streaming)".

IMPORTANT: Verify the exact node NAME used when the RMS creator is
registered in rms_workflow/graph.py. Open graph.py, find
add_node(...) for the creator, and use that EXACT string. If it's
registered as "creator_agent", add "creator_agent". If
"creator_agent_node", add that. Add both to be safe.

DELIVERABLE
Show the node name from graph.py (the add_node call for creator).
Show the updated static_output_nodes list(s). Confirm both lists
updated if there are two.
═══════════════════════════════════════════════════════════════════