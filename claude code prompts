═══════════════════════════════════════════════════════════════════
TASK 6.6-FIX: Expand MRR CREATOR_KEYWORDS for natural phrasings
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/workflows/material_workflow/intent.py and
  app/workflows/material_workflow/prompts.py (the lists in both
  must stay in sync — same pattern as Task 6.4).
- Only ADD keywords; do not remove existing ones.

PROBLEM
"create an mrr" was classified OUT_OF_DOMAIN. CREATOR_KEYWORDS lacks
the common shorthand "mrr" and natural phrasings users actually type.

FIX
Append these creator phrasings to CREATOR_KEYWORDS in BOTH files:

    "create an mrr",
    "create a mrr",
    "create mrr",
    "new mrr",
    "raise mrr",
    "raise an mrr",
    "start an mrr",
    "start mrr",
    "make an mrr",
    "make a mrr",
    "i want to raise an mrr",
    "i want to create an mrr",
    "want to create mrr",
    "i need to raise an mrr",
    "need to create mrr"

Verify by reading the existing CREATOR_KEYWORDS list. Do NOT
duplicate any entry already present.

Also verify ordering in the classifier: OUT_OF_DOMAIN must not
match these phrases before CREATOR does. If the classifier checks
OUT_OF_DOMAIN keywords first and any of them contain a substring
that matches the new phrases, those need to come AFTER CREATOR
matching (the keyword classifier order matters — earlier match
wins per Task 5.6).

DELIVERABLE
Show:
- The added entries appended to CREATOR_KEYWORDS in both files.
- Confirmation that the matching order in classify_mrr_intent
  still gives CREATOR priority for these phrases (no OUT_OF_DOMAIN
  collision).
═══════════════════════════════════════════════════════════════════