═══════════════════════════════════════════════════════════════════
TASK 6.4-FIX: Hoist execution_log assignment before Excel early-exit
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/workflows/rms_workflow/nodes.py
- Only move/duplicate the execution_log assignment so it's defined
  before any Excel early-exit branch
- Do NOT change any other logic

PROBLEM
The Excel early-exit in creator_agent_node (and likely
reviewer_agent_node and approver_agent_node) references
`execution_log` before it's assigned. Server crashes with:
  "cannot access local variable 'execution_log' where it is not
  associated with a value"

FIX
In ALL THREE persona functions (creator_agent_node,
reviewer_agent_node, approver_agent_node):

Locate the line:
    execution_log = state.get("execution_log", [])

Move it to BEFORE the `uploaded_files = state.get("uploaded_files") or []`
and `if uploaded_files: return _handle_..._excel(...)` lines.

So the structure becomes:
    # ... entry print ...
    # ... get last_user_message ...
    current_emp_no = state.get("emp_no", state.get("user_id", ""))
    execution_log = state.get("execution_log", [])   # ← HOIST THIS HERE

    uploaded_files = state.get("uploaded_files") or []
    if uploaded_files:
        return _handle_<persona>_excel(state, uploaded_files, current_emp_no, execution_log)

    # ... rest of chat path ...

Apply identically in all three node functions.

DELIVERABLE
Show the ordering for each of the three node functions (the first
~15 lines of each), confirming execution_log is assigned BEFORE the
uploaded_files check.
═══════════════════════════════════════════════════════════════════