print(f"[DIAG-UPLOAD] Parsed file: filename={parsed.get('filename')}, "
      f"file_type={parsed.get('file_type')}, "
      f"row_count={parsed.get('row_count')}, "
      f"first_row={parsed.get('data', [{}])[0] if parsed.get('data') else None}")

print(f"[DIAG-EXCEL-HANDLER] uploaded_files count: {len(state.get('uploaded_files', []))}, "
      f"types: {[f.get('file_type') for f in state.get('uploaded_files', [])]}")

print(f"[DIAG-VALIDATOR] valid={result.get('valid')}, "
      f"errors={result.get('errors')}, "
      f"warnings={result.get('warnings')}")