# TASK 4.7.3 REPORT: Fix HRMS + MRR agent node collection checks

## INTENDED / ACTUAL FOOTPRINT

- **Intended:** Modify `app/workflows/hrms_workflow/nodes.py` and `app/workflows/material_workflow/nodes.py` to change `selected_collection == "HRMS"` / `selected_collection == "MRR"` conditions.
- **Actual:** Neither file contains the legacy conditions `selected_collection == "HRMS"` or `selected_collection == "MRR"`. **No files were modified.**

## HRMS FINDINGS

### `creator_agent_node` (line 289)
- **No legacy condition found.** The function has no `if selected_collection == "HRMS":` guard. The LLM binding logic runs unconditionally.

### `reviewer_agent_node` (line 419)
- **No legacy condition found.** Same as above — no collection check.

### `approver_agent_node` (line 590)
- **No legacy condition found.** Same as above — no collection check.

## MRR FINDINGS

### Functions examined in `material_workflow/nodes.py`:

| Function | Legacy condition found? | Notes |
|---|---|---|
| `creator_agent_node` | Does not exist | No such function in this file |
| `approver_agent_node` | No | Direct DB query, no LLM streaming, no collection check |
| `erp_approver_agent_node` | No | Direct DB query, no LLM streaming, no collection check |
| `issuer_agent_node` | No | Simple DB/user-check, no collection check |
| `evaluate_and_route` | No | Direct DB insert, no collection check |
| `auto_verifier_node` | No | Direct DB query, no collection check |
| `handle_header` | No | Uses LLM extractors but not via `selected_collection` guard |
| `handle_items` | No | Direct item extraction, no collection check |
| `excel_handler_node` | No | Direct DB resolution, no collection check |
| `mrr_submission_node` | No | Direct DB inserts, no collection check |
| All other nodes | No | None contain `selected_collection == "MRR"` or `selected_collection ==` |

## DEVIATIONS

- The assumed bug pattern from Task 4.7.2 (legacy `== "HRMS"` / `== "MRR"` guards) does not exist in these two workflow files.
- HRMS agent nodes have always run unconditionally (they don't gate on `selected_collection`).
- MRR agent nodes also lack any `selected_collection` equality check — they operate via direct DB queries or sub-workflows.
- No DIAG print statements were added because there was no condition to change and the task specified adding them only alongside condition changes.

## CONFIRMATION

- No files were modified.
- No other logic was touched.
- RMS workflow (Task 4.7.2) remains the only set of files that required this fix.
