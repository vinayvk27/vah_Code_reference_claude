# TASK 4.2.1 — Fix RMS message accumulation using add_messages channel

## INTENDED FOOTPRINT

| Action | File |
|--------|------|
| READ | `app/workflows/rms_workflow/state.py` |
| READ | `app/workflows/rms_workflow/nodes.py` |
| READ | `app/workflows/rms_workflow/graph.py` |
| MODIFY | `app/workflows/rms_workflow/state.py` |
| MODIFY | `app/workflows/rms_workflow/nodes.py` |
| CREATE | `TASK_4_2_1_REPORT.md` |

## ACTUAL FOOTPRINT

| Action | File | Status |
|--------|------|--------|
| READ | `app/workflows/rms_workflow/state.py` | Done |
| READ | `app/workflows/rms_workflow/nodes.py` | Done |
| READ | `app/workflows/rms_workflow/graph.py` | Done |
| MODIFY | `app/workflows/rms_workflow/state.py` | Done |
| MODIFY | `app/workflows/rms_workflow/nodes.py` | Done |
| CREATE | `TASK_4_2_1_REPORT.md` | Done |

No files outside the intended footprint were touched.

## NODE FUNCTIONS MODIFIED

| # | Node Function | Change |
|---|---------------|--------|
| 1 | `permission_blocked_node` (line ~150) | `"messages": messages + [ai_message]` → `"messages": [ai_message]` |
| 2 | `creator_agent_node` (line ~574) | `"messages": messages + [AIMessage(content=full_text)]` → `"messages": [AIMessage(content=full_text)]` |
| 3 | `reviewer_agent_node` (line ~825) | `"messages": messages + [AIMessage(content=full_text)]` → `"messages": [AIMessage(content=full_text)]` |
| 4 | `approver_agent_node` (line ~1059) | `"messages": messages + [AIMessage(content=full_text)]` → `"messages": [AIMessage(content=full_text)]` |

## BEFORE/AFTER OF EACH RETURN STATEMENT

### 1. `permission_blocked_node` (state.py:150-153)

**Before:**
```python
return {
    "messages": messages + [ai_message],
    "execution_log": execution_log
}
```

**After:**
```python
return {
    "messages": [ai_message],
    "execution_log": execution_log
}
```

### 2. `creator_agent_node` (nodes.py:574-577)

**Before:**
```python
return {
    "messages": messages + [AIMessage(content=full_text)],
    "execution_log": execution_log
}
```

**After:**
```python
return {
    "messages": [AIMessage(content=full_text)],
    "execution_log": execution_log
}
```

### 3. `reviewer_agent_node` (nodes.py:825-828)

**Before:**
```python
return {
    "messages": messages + [AIMessage(content=full_text)],
    "execution_log": execution_log
}
```

**After:**
```python
return {
    "messages": [AIMessage(content=full_text)],
    "execution_log": execution_log
}
```

### 4. `approver_agent_node` (nodes.py:1059-1062)

**Before:**
```python
return {
    "messages": messages + [AIMessage(content=full_text)],
    "execution_log": execution_log
}
```

**After:**
```python
return {
    "messages": [AIMessage(content=full_text)],
    "execution_log": execution_log
}
```

## STATE.PY CHANGES

Added imports:
```python
from typing import Annotated
from langgraph.graph.message import add_messages
```

Changed field:
```python
# Before
messages: list

# After
messages: Annotated[list, add_messages]
```

## NODES LEFT UNCHANGED (as required)

- `parse_intent_node` — returns no messages, only routing/execution_log state.
- `permission_check_node` — returns no messages, only `has_permission`/`denial_message`/`execution_log`.
- `permission_router` — returns a string route key, no messages.

## DEVIATIONS

None. All changes match the task specification exactly.
