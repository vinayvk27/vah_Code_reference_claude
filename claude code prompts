═══════════════════════════════════════════════════════════════════
TASK 6.3-FIX: Add RMS approver to static_output_nodes
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/controllers/chat_controller.py
- Only add node names to the existing static_output_nodes list(s)
- Do NOT change any other logic

CONTEXT
approver_agent_node is now procedural (no LLM streaming), like the
RMS creator and reviewer. Its returned message will not appear in the
UI unless its node name is in static_output_nodes.

CHANGE
There are TWO static_output_nodes lists in chat_controller.py — one
in stream_chat_with_orchestrator and one in stream_chat_with_file.
Update BOTH.

Add these entries (near the existing RMS creator/reviewer entries):
    "approver_agent",
    "approver_agent_node",
with comment "# RMS procedural approver (no LLM streaming)".

VERIFY the exact node name from rms_workflow/graph.py — open graph.py,
find the add_node(...) call for the approver, and use that EXACT
string. Add both variants ("approver_agent" and "approver_agent_node")
to be safe.

IMPORTANT: Do NOT confuse this with MRR's approver nodes
("mrr_approver_agent" / "mrr_approver_agent_node") which are already
in the list. Add the RMS approver names specifically.

DELIVERABLE
Show the approver node name from rms_workflow/graph.py and the
updated static_output_nodes list(s). Confirm both lists were updated
and that RMS creator + reviewer + approver are all now present.
═══════════════════════════════════════════════════════════════════