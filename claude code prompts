═══════════════════════════════════════════════════════════════════
TASK 5.8-FIX: Add creator_needs_file to static_output_nodes
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/controllers/chat_controller.py
- Only add the new node name to the existing static_output_nodes list
- Do NOT modify any other logic

CHANGE
In stream_chat_with_orchestrator, find the static_output_nodes list.
Add two strings to it:
  "creator_needs_file"
  "creator_needs_file_node"

Place them in the MRR section of the list (next to excel_handler,
submit_mrr, cancel_mrr).

DELIVERABLE
Show the modified list. Confirm no other changes.
═══════════════════════════════════════════════════════════════════