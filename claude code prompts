SELECT r.Emp_no, r.Name, r.Department, r.GroupIDx, r.IsActive,
       ur.role_id, dr.role_name
FROM resources r
JOIN dpah_user_roles ur ON ur.emp_id = r.Emp_no AND ur.is_active = 1
JOIN dpah_roles dr ON dr.role_id = ur.role_id
WHERE r.IsActive = 1
ORDER BY ur.role_id, r.Emp_no
LIMIT 20;