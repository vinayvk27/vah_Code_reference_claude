# 1. Confirm where get_current_user is actually defined
findstr /S /N "def get_current_user" app\

# 2. Confirm where oauth2_scheme is defined
findstr /S /N "oauth2_scheme" app\

# 3. Show the imports at the top of chat_route.py
powershell "Get-Content app\routes\chat_route.py -TotalCount 25"

# 4. Show the imports at the top of auth_route.py
powershell "Get-Content app\routes\auth_route.py -TotalCount 25"

# 5. Show the full security.py file (it's small)
type app\core\security.py

# 6. Show the full dependencies.py file (if it exists)
type app\core\dependencies.py

# 7. List everything in app/core
dir app\core