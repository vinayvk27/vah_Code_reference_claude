Done. Here's the report:

Existing working code in mariadb_queries.py:203-206:


SELECT ITEMID, ITEMREVISIONID, UOM, QOH, PRICE, DESCRIPTION
FROM com_search
WHERE DP_PART_NO = :pn AND WAREHOUSE = '1'
This shows com_search has: ITEMID, ITEMREVISIONID, UOM, QOH, PRICE, DESCRIPTION, DP_PART_NO, WAREHOUSE — but no ITEM_NAME or LPP_VALUE_UNITPRICE_INR. Those are on SCMITEMS (LPP lives there).

Fixes applied:

nodes.py:1108-1122 — _lookup_item now selects DP_PART_NO, DESCRIPTION (alias → item_name), PRICE (alias → unit_price_rs).

mariadb_queries.py:326-328 — get_item_availability also fixed: ITEM_NAME → DESCRIPTION (same table schema issue).

The existing resolve_item_from_partno (line 203) already used correct columns, confirming the fix pattern.
