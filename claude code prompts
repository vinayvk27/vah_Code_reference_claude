Done. Here's the report:

Functions in mariadb_queries.py:
get_project_by_number, get_project_phase, get_max_matrequisition_id, get_max_matrequisition_line_id, get_max_mrr_sequence_for_fy, get_existing_mrr_numbers_count, resolve_item_from_partno, get_item_lpp, get_item_uomid, get_item_id_by_name

Broken import in tools.py:99:


from app.services.mariadb_queries import get_item_availability, get_item_lpp
Root cause: The function get_item_availability was never defined in mariadb_queries.py — it existed in oracle_queries.py but was never migrated/added to the MariaDB module. This caused ImportError when get_effective_item_availability (called by get_item_details_from_db) tried to import it.

Fix: Added get_item_availability(item_id: int) to mariadb_queries.py:316-342 — queries com_search for ITEM_NAME and QOH by ITEMID. This mirrors the Oracle version's contract (returns {"name", "onhand_quantity"} or None), which is what tools.py:111-114 expects. No changes to tools.py were needed.

data: {"type": "meta", "thread_id": "fb778952-fcfd-4455-97b9-a7b516a1e536"}

data: {"type": "chunk", "text": "I couldn't submit the form. Please fix the highlighted fields and try again.\n\n"}

data: {"type": "form_errors", "form_id": "mrr_create_form", "errors": {"items.0.item_name": "Item lookup failed: (mysql.connector.errors.ProgrammingError) 1054 (42S22): Unknown column 'l.REQUANTITY' in 'field list'\n[SQL: \n            SELECT COALESCE(SUM(l.REQUANTITY), 0) as pending_qty\n            FROM mrr_matrequisitionlines l\n            JOIN mrr_matrequisitionheader h ON l.MATREQUISITIONID = h.MATREQUISITIONID\n            WHERE l.ITEMID = %(item_id)s AND h.VERIFICATION_STATUS = 'APPROVED' AND h.APPROVAL_STATUS = 'PENDING'\n        ]\n[parameters: {'item_id': '7636'}]\n(Background on this error at: https://sqlalche.me/e/20/f405)"}}

data: [DONE]

** cant we fix the errors at once because i dont know why these errors coming now only because excel upload is also uisng same functions then why now only 
