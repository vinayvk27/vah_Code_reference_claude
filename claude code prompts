═══════════════════════════════════════════════════════════════════
TASK 6.7-FIX5: Align MRR INSERT with actual mrr_matrequisitionheader schema
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Inspect the real schema first.
- Fix the INSERT statement in form_submit_handler_node (nodes.py).
- ALSO fix evaluate_and_route's INSERT if it has the same bug.
- Do NOT modify other files.

INVESTIGATION
Run:
    DESCRIBE mrr_matrequisitionheader;
on the MariaDB database. List EVERY column name with its type.

Cross-check each column referenced in the form_submit_handler INSERT
statement against the real schema:
  MATREQUISITIONNO, STATUS, VERIFICATION_STATUS, VERIFICATION_DATE,
  APPROVAL_STATUS, APPROVAL_DATE, ERP_APPROVAL_STATUS, ERP_APPROVAL_DATE,
  SUPPLIERID, SUPPLIERSITEID, REQTRANSACTIONTYPE, COMMENTS,
  DEPARTMENTID, REQUESTORID, REQUISITIONDATE, TOTAL_COST,
  PROJECTID, PHASEID

For each one not in the real schema, find the actual name (if any).

Also DESCRIBE mrr_matrequisitionlines and verify the line INSERT
columns:
  MATREQUISITIONID, ITEMID, ITEMNAME, REQUANTITY, ITEMCOST,
  UOMID, ITEMREVISIONID, ISSUEQTY, RETURNQUANTITY, CLOSEREASON

FIX
Two places to update in app/workflows/material_workflow/nodes.py:

1. form_submit_handler_node — the header INSERT and line INSERT.
2. evaluate_and_route — the same header INSERT and line INSERT.

Both use identical SQL today. Update both to match the real schema.
Use the same column names and value mappings; just fix names that
don't exist.

KNOWN GUIDANCE from architecture history:
  - DEPARTMENTID was renamed to DEPARTMENT (varchar(50)).
    Map the value '1' to whatever DEPARTMENT expects. If the field
    stores a department code like 'EAG', use the user's dept from
    state (state.get('user_info', {}).get('dept', 'EAG')). For
    safety, pass the user's dept rather than a hard-coded value.
  - TOTAL_COST is the correct column name (used by approver query
    today). Leave as-is.

For any other unknown columns, prefer the simplest substitution:
  - If a column doesn't exist and is non-essential (like SUPPLIERID,
    SUPPLIERSITEID), drop it from the INSERT entirely.
  - If a column exists but with a slightly different name, use the
    real name.

DELIVERABLE
- Paste DESCRIBE mrr_matrequisitionheader (every column + type).
- Paste DESCRIBE mrr_matrequisitionlines (every column + type).
- Show the corrected INSERT in form_submit_handler_node (final SQL).
- Show the corrected INSERT in evaluate_and_route (final SQL).
- List any columns dropped or renamed in the fix.
- Confirm both INSERTs now match the real schema.
═══════════════════════════════════════════════════════════════════