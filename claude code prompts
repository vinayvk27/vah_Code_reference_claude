═══════════════════════════════════════════════════════════════════
TASK 7.0-FIX: Switch to MemorySaver (no extra dependencies)
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/workflows/material_workflow/graph.py

CHANGE
Replace the SqliteSaver setup with MemorySaver:

Remove:
    import sqlite3
    from langgraph.checkpoint.sqlite import SqliteSaver
    _mrr_conn = sqlite3.connect("checkpoints_mrr.db", check_same_thread=False)
    _mrr_checkpointer = SqliteSaver(_mrr_conn)

Add:
    from langgraph.checkpoint.memory import MemorySaver
    _mrr_checkpointer = MemorySaver()

Everything else stays the same:
    graph_builder.compile(checkpointer=_mrr_checkpointer)

DELIVERABLE
Show the updated graph.py imports and checkpointer line.
═══════════════════════════════════════════════════════════════════