-- Check how many roles emp 16 currently has
SELECT emp_id, role_id FROM dpah_user_roles 
WHERE emp_id = '16' AND is_active = 1;

# Find build_user_context_by_empno
# Look for where role_ids are fetched
# It should be something like:
SELECT role_id FROM dpah_user_roles WHERE emp_id = :emp_id AND is_active = 1

