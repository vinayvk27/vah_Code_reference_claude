C:\ml_env_311\Scripts\python.exe -c "from app.dao.database import engine; from sqlalchemy import inspect; ins=inspect(engine); [print(t, [c['name'] for c in ins.get_columns(t)]) for t in ['hr_leave_types_demo','hr_leave_requests_demo','hr_payslips_demo']]"
