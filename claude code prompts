The mrr_submission_node Excel path also has WAREHOUSEID — need to check if that's a real column. It is (from DESCRIBE). That node already includes MATREQUISITIONID in the INSERT correctly. But it references WAREHOUSEID which IS a real column, and REQUISITIONTYPE which is also real. That node looks fine schema-wise — let me just verify REQUESTORID casting there.

The mrr_submission_node uses header["requestor_id"] from the validated Excel data which should already be a number from the validator. It's already fine. No changes needed there.

Done. Here's the consolidated report:

SHOW CREATE TABLE key findings:

MATREQUISITIONID is bigint(20) NOT NULL with PRIMARY KEY — NOT AUTO_INCREMENT. Must explicitly provide it.
Max current ID: 16, total rows: 17 (PK collision risk with manual sequence)
New helpers — nodes.py:68-102:

_lookup_project(db, project_code) — queries posproject WHERE PROJECTNUMBER = :code, returns {"project_id": int, "description": str}.

_lookup_latest_phase(db, project_id) — queries posprojectphases WHERE PROJECTID = :pid ORDER BY PROJECTPHASEID DESC LIMIT 1, returns PROJECTPHASEID or None.

form_submit_handler_node INSERT — nodes.py:1300-1315:

Now includes MATREQUISITIONID (=computed header_id), DESCRIPTION (auto-fetched from posproject)
Column order matches real schema exactly
PK computed before INSERT: seq + 1 → header_id → mrr_no
evaluate_and_route INSERT — nodes.py:635-650:

Same fixes: explicit MATREQUISITIONID, DESCRIPTION column, PROJECTPHASEID (not PHASEID), DEPARTMENT (not DEPARTMENTID)
REQUESTORID cast: int(user_id) if str(user_id).isdigit() else 0
DEPARTMENT sourced from user_info.dept (default "EAG")
Fixes summary:

Issue	Fix
Project code string → bigint ID	_lookup_project() resolves via posproject
Description not stored	Auto-fetched into DESCRIPTION column
Phase not resolved	_lookup_latest_phase() queries posprojectphases
MATREQUISITIONID = 0	Explicit next_id + include in INSERT (not AUTO_INCREMENT)
REQUESTORID string → bigint	Cast: int(user_id) if str(user_id).isdigit() else 0
DEPARTMENT missing	Sourced from state.user_info.dept
