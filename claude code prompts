═══════════════════════════════════════════════════════════════════
TASK 5.6-FIX: Lowercase persona values in MRR keyword classifier
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/workflows/material_workflow/intent.py
- ONLY change the persona string values from UPPERCASE to lowercase
  in the _build_in_domain calls
- Do NOT change any other logic, keyword lists, function structure,
  or anything else

CHANGES
In classify_mrr_intent:
- "ERP_APPROVER" → "erp_approver"
- "ISSUER" → "issuer"
- "APPROVER" → "approver"
- "CREATOR" → "creator" (in both Step 5 and Step 6 default)

All four persona string literals must become lowercase to match
what permission_check_node compares against.

DELIVERABLE
Show the modified classify_mrr_intent function in your reply.
═══════════════════════════════════════════════════════════════════