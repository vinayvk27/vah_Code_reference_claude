═══════════════════════════════════════════════════════════════════
TASK 8.0: Move MRR approver/erp_approver permissions from
          hardcoded approvers.py to DB-driven AuthController
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Modify ONLY:
    app/workflows/material_workflow/nodes.py
      (permission_check_node — approver/erp_approver/issuer branches)
    scripts/seed_dpah_tables.py or a new migration SQL file
      (add agent rows + role_agent_access rows)
- Do NOT modify AuthController, auth_controller.py, or RMS/HRMS.
- Do NOT delete approvers.py yet — leave it as a reference.
- Output INTENDED FOOTPRINT at start, list modified files at end.

PART A — INVESTIGATION (report only, no code change)
─────────────────────────────────────────────────────
1. Run:
   SELECT agent_id, agent_name, is_active
   FROM dpah_agents WHERE agent_name LIKE 'MRR%';

   Report every MRR agent row that exists. Note which of these
   are missing:
     MRR_GENERAL_APPROVER
     MRR_GENERAL_ERP_APPROVER

2. Run:
   SELECT ra.*, a.agent_name
   FROM dpah_role_agent_access ra
   JOIN dpah_agents a ON ra.agent_id = a.agent_id
   WHERE a.agent_name LIKE 'MRR%';

   Report current role-agent mappings for MRR agents.

3. Read permission_check_node (nodes.py). Quote the exact
   approver/erp_approver/issuer branches as they exist today.

4. Read approvers.py. Quote MANAGER_APPROVERS and ERP_APPROVERS
   lists — these give us the emp_no values to seed.

PART B — SEED DB (SQL statements)
──────────────────────────────────
Based on Part A findings:

1. If MRR_GENERAL_APPROVER agent row is missing, insert it:
   INSERT INTO dpah_agents (agent_name, module_id, is_active, ...)
   VALUES ('MRR_GENERAL_APPROVER', 5, 1, ...);
   Use module_id=5 (MRR). Match the column structure of existing
   MRR agent rows.

2. If MRR_GENERAL_ERP_APPROVER agent row is missing, insert it:
   Same pattern.

3. For each emp_no in MANAGER_APPROVERS (e.g., "16", "5"):
   Find their role_id and group_idx from dpah_user_roles.
   Insert into dpah_role_agent_access:
   INSERT INTO dpah_role_agent_access (role_id, group_id, agent_id)
   SELECT ur.role_id, ur.group_idx, a.agent_id
   FROM dpah_user_roles ur, dpah_agents a
   WHERE ur.emp_id IN ('16', '5')
     AND a.agent_name = 'MRR_GENERAL_APPROVER'
   ON DUPLICATE KEY UPDATE agent_id = agent_id;

4. Same for ERP_APPROVERS ("531") → MRR_GENERAL_ERP_APPROVER.

Run all SQL against the live DB and confirm row counts.

PART C — MODIFY permission_check_node
──────────────────────────────────────
In nodes.py, find the approver/erp_approver/issuer branches in
permission_check_node. They currently call:
    is_manager_approver(emp_no)
    is_erp_approver(emp_no)
    is_issuer(emp_no)

Replace ALL THREE with the same Gate 2 DB check used for creator:

    # For approver, erp_approver, issuer:
    # Build expected agent name same as creator pattern
    collection_suffix = selected_collection.upper().replace("MRR_", "")
    expected_agent_name = f"MRR_{collection_suffix}_{selected_agent_role.upper()}"

    db = SessionLocal()
    try:
        agent_row = db.query(Agent).filter(
            Agent.agent_name == expected_agent_name,
            Agent.is_active == 1
        ).first()

        if not agent_row:
            return {"has_permission": False, "denial_message": "..."}

        agent_or_none = AuthController.check_agent_access(
            emp_no=emp_no,
            group_idx=group_idx,
            role_ids=role_ids,
            agent_id=agent_row.agent_id,
            db=db
        )
        if agent_or_none is None:
            return {"has_permission": False, "denial_message": "..."}

        return {"has_permission": True, "denial_message": None, ...}
    finally:
        db.close()

Replace each of the three separate if-blocks with this unified
pattern. Use appropriate denial messages per role.

PART D — REMOVE redundant node-level checks
────────────────────────────────────────────
In approver_agent_node (nodes.py ~line 1035):
    Remove the call to is_manager_approver(emp_no) and its
    associated denial return. Permission is already guaranteed
    by permission_check_node.

In erp_approver_agent_node (nodes.py ~line 1242):
    Remove the call to is_erp_approver(emp_no) and its
    associated denial return. Same reason.

Do NOT remove from issuer_agent_node if it exists — check
whether it has a similar redundant check and remove if present.

PART E — VERIFY
───────────────
After changes, run:
    SELECT u.emp_id, a.agent_name
    FROM dpah_user_roles u
    JOIN dpah_role_agent_access ra ON u.role_id = ra.role_id
      AND u.group_idx = ra.group_id
    JOIN dpah_agents a ON ra.agent_id = a.agent_id
    WHERE u.emp_id IN ('5', '16', '531')
      AND a.agent_name LIKE 'MRR%';

This should show emp 16 and 5 mapped to MRR_GENERAL_APPROVER,
and emp 531 mapped to MRR_GENERAL_ERP_APPROVER.

Also verify the login /me/access response for emp 16 now shows
MRR_GENERAL_APPROVER in their agents list (meaning the frontend
will display approver keywords for them automatically).

DELIVERABLE
Create TASK_8_0_REPORT.md with:
- Part A findings (existing agents + mappings).
- Part B SQL statements run + row counts confirmed.
- Part C: before/after of permission_check_node for all three roles.
- Part D: confirmation redundant checks removed from both nodes.
- Part E: verification query result.
- Confirmation: approvers.py NOT deleted (kept for reference).
- Confirmation: no changes to AuthController, RMS, HRMS.
═══════════════════════════════════════════════════════════════════