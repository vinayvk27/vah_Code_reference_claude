All tasks are complete. Here's a summary of every change:

Section 1 — Fix: form_errors not displaying in chat
Root cause found: The form_errors handler in useChatStream.js:171-184 was correctly setting formErrors on the message object. The issue was that FormRenderer.jsx had the getError() helper and error display logic, but the input styling for error states and compact inline error text wasn't consistently applied across all field types — and the overall error banner was always visible (not gated by Object.keys(formErrors).length > 0).

Fixes applied:

useChatStream.js:247 — Changed formErrors = null to formErrors = {} in submitForm() so stale errors are cleared before re-submission (truthy check for error banner works correctly)
FormRenderer.jsx — Rewrote field rendering:
Scalar fields: inputs now get border-red-500 ring-1 ring-red-500/20 when error exists
Error text below each field: <p className="mt-0.5 text-[11px] text-red-400 flex items-center gap-1"><span>⚠</span><span>{error}</span></p>
Table cell errors: same treatment with dot-notation paths (items.0.item_name)
Global error banner now gated: {formErrors && Object.keys(formErrors).length > 0 && ...}
Section 2 — Redesign: Compact chat-native forms
File changed: FormRenderer.jsx — Complete rewrite of styling.

Key changes:

Wrapper: w-full max-w-[380px] rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800/60 p-3 my-1
Title: text-xs font-bold uppercase tracking-widest text-slate-500
Labels: text-[11px] font-semibold uppercase tracking-wide
Inputs: px-2.5 py-1.5 text-[13px] with subtle bg-slate-50 dark:bg-slate-700/50 backgrounds
Layout: Single-column flex flex-col gap-2 (no grid)
Textarea: rows={3} (was 5)
Footer buttons: Side-by-side right-aligned, px-3/px-4 py-1.5 text-xs, compact
Submitted state: Compact chip <CheckCircle2 size={12} /> Form submitted
Cancelled state: <div className="text-xs text-slate-500 italic">Form cancelled.</div>
Table rows: text-xs, 32px height, minimal padding
Add Row button: text-[11px] with Plus size={12}
Section 3 — Fix Known Gaps
Gap	File Changed	What
GAP 3	api.js:65-72	Added authService.getCapabilities() method
GAP 4	Sidebar.jsx:119-138	Replaced handleComingSoon buttons with NavLink to /dashboard/chat-history and /dashboard/settings; removed unused handleComingSoon function and toast import
GAP 1+2	App.jsx:9-10, 27-28	Lazy-loaded ChatHistory and Settings components, wired into route tree with Suspense (handled by DashboardLayout wrapper)
GAP 10	useChatStream.js	Removed 2 console.log("Stream stopped by user.") debug statements. InteractiveChatTable.jsx had none. DashboardLayout.jsx console.log was already removed in a prior pass
GAP 6	DashboardLayout.jsx:31-34	Added toast.error('Could not refresh permissions. Some features may be unavailable.') in the refreshAccess catch block, plus import { toast } from 'sonner'
Requisition Type input field itself is not coming earlier a radio button will be there to select eithe returnable or non returnable check why it is not coming
Grep "requisition|returnable|non.returnable|Requisition" (in c:\xampp1\htdocs\hemish\VAH_Chatbot_1\src\components\FormRenderer.jsx)
1 line of output
The "Requisition Type" field isn't in the form rendering code at all — FormRenderer.jsx doesn't have any special handling for it. Let me check what field types FormRenderer currently supports and how the backend sends form specs.

Read c:\xampp1\htdocs\hemish\VAH_Chatbot_1\src\components\FormRenderer.jsx
Now I see the issue. The backend sends a form where Requisition Type is likely a field with a type that maps to radio buttons (like type: "radio" or type: "radio_group"), but the current FormRenderer.jsx only handles these types in the scalar fields section:

text, number, date (line 235)
textarea (line 304)
select (line 317)
checkbox (line 333)
There's no handler for radio or radio_group field types, so those fields silently render nothing.

Let me check the original file to confirm this was present before my rewrite.

Grep "radio" (in c:\xampp1\htdocs\hemish\VAH_Chatbot_1)
5 lines of output
Grep "radio" (in c:\xampp1\htdocs\hemish\VAH_Chatbot_1\DPAH_FRONTEND_DOCUMENTATION.md)
1 line of output
Read c:\xampp1\htdocs\hemish\VAH_Chatbot_1\DPAH_FRONTEND_DOCUMENTATION.md (lines 601-630)
Grep "radio" (in c:\xampp1\htdocs\hemish\VAH_Chatbot_1\DPAH_FRONTEND_DOCUMENTATION.md)
1 line of output
Grep "requisition|returnable|non.returnable|Requisition|requisition_type|Requisition Type" (in c:\xampp1\htdocs\hemish\VAH_Chatbot_1\DPAH_FRONTEND_DOCUMENTATION.md)
1 line of output
Grep "returnable|requisition" (in c:\xampp1\htdocs\hemish\VAH_Chatbot_1\DPAH_FRONTEND_DOCUMENTATION.md)
1 line of output
I can see the issue. In FormRenderer.jsx:235-313, the scalar field rendering only handles these types: ['text', 'number', 'date'], textarea, select, and checkbox. There's no handler for radio type fields.

When the backend sends a requisition_type field with type: "radio" (with options like "returnable" and "non_returnable"), FormRenderer.jsx renders nothing for it because it falls through all the conditional branches and renders nothing.

Let me fix this by adding a radio group renderer.

Read c:\xampp1\htdocs\hemish\VAH_Chatbot_1\src\components\FormRenderer.jsx (lines 317-331)
I see the problem. The form field rendering in [FormRenderer.jsx only handles select, text, textarea, checkbox — but not radio. The backend sends a field with type: "radio" for the "Requisition Type" (returnable / non-returnable), but FormRenderer.jsx has no renderer for field.type === 'radio'.


** mostly its fine but need the best UI form looks basic and very compact increase width it a bit more and also fonts and colour everything looks basic since we are using uses will expect best controls i hope you understood.
