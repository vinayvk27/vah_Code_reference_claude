═══════════════════════════════════════════════════════════════════
TASK 5.9-FIX: Expand RMS reviewer keywords for hour-logging phrases
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/workflows/rms_workflow/intent.py
- Only modify the REVIEWER_KEYWORDS list
- Do NOT change any other logic

CHANGE
Add these entries to REVIEWER_KEYWORDS (keep all existing ones):
    "log",
    "hours",
    "hrs",
    "worked",
    "time spent",
    "spent",

These catch phrasings like "log 8 hours today on task 1",
"I spent 6 hrs", "worked 4 hours" where numbers/words sit between
the keyword tokens.

NOTE on ordering: REVIEWER is checked AFTER approver and creator, so
adding broad terms like "hours" won't steal approver/creator messages
(those are matched first). Verify this ordering is preserved.

DELIVERABLE
Show the updated REVIEWER_KEYWORDS list. Confirm ordering of checks
(approver → creator → reviewer) is unchanged.
═══════════════════════════════════════════════════════════════════