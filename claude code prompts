═══════════════════════════════════════════════════════════════════
TASK 6.2-FIX: Add RMS reviewer to static_output_nodes
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/controllers/chat_controller.py
- Only add node names to the existing static_output_nodes list(s)
- Do NOT change other logic

CONTEXT
reviewer_agent_node is now procedural (no LLM streaming). Like the
creator, its output won't appear in the UI unless its node name is
in static_output_nodes.

CHANGE
In BOTH static_output_nodes lists (stream_chat_with_orchestrator and
stream_chat_with_file), add:
    "reviewer_agent",
    "reviewer_agent_node",
near the creator entries, with comment "# RMS procedural reviewer".

Verify the exact node name from rms_workflow/graph.py add_node call
for the reviewer and use that exact string (add both variants to be
safe).

DELIVERABLE
Show the reviewer node name from graph.py and the updated list(s).
═══════════════════════════════════════════════════════════════════