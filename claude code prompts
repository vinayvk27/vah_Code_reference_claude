ENVIRONMENT: Do NOT run verification commands. User verifies manually.

TASK H2.3d: Update app/workflows/hrms_workflow/tools.py to re-export all 9 tools. This is a 1-file, 1-block change.

REPLACE the entire contents of app/workflows/hrms_workflow/tools.py with:

  """
  HRMS Workflow Tools
  ===================
  Thin wrappers around hrms_workflow.hrms_tools.py.
  Re-exports tool functions for use in nodes.
  """
  
  from app.workflows.hrms_workflow.hrms_tools import (
      tool_list_my_leave_balance,
      tool_request_leave,
      tool_my_payslip,
      tool_list_my_leave_requests,
      tool_create_leave_type,
      tool_create_payslip,
      tool_list_pending_leaves,
      tool_decide_leave,
      tool_view_any_payslip,
  )

ALSO update app/workflows/hrms_workflow/__init__.py to re-export the 9 tools at the package level. Find the existing __init__.py content and ADD:

  from app.workflows.hrms_workflow.tools import (
      tool_list_my_leave_balance,
      tool_request_leave,
      tool_my_payslip,
      tool_list_my_leave_requests,
      tool_create_leave_type,
      tool_create_payslip,
      tool_list_pending_leaves,
      tool_decide_leave,
      tool_view_any_payslip,
  )

And update __all__ to include all 9 tool names alongside the existing exports.

REPORT BACK: Confirm both files updated.


C:\ml_env_311\Scripts\python.exe -c "from app.workflows.hrms_workflow import tool_request_leave, tool_decide_leave, tool_create_leave_type; print('All 9 tools accessible from package level')"
