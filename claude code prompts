═══════════════════════════════════════════════════════════════════
TASK 6.1-FIX: Restore missing 'tool' import in rms nodes.py
═══════════════════════════════════════════════════════════════════

CRITICAL CONSTRAINT
- Only touch app/workflows/rms_workflow/nodes.py
- Only fix the import; do not change any function logic

PROBLEM
reviewer_agent_node (and possibly approver_agent_node) crash with
"name 'tool' is not defined". The Task 6.1 cleanup removed the
'tool' import that these nodes still use via @tool decorators.

FIX
At the top of nodes.py, ensure this import is present:
    from langchain_core.tools import tool

Verify nothing else was removed that reviewer_agent_node or
approver_agent_node still references. Specifically check these are
still imported (add back any that are missing):
    from langchain_core.messages import HumanMessage, AIMessage, SystemMessage, ToolMessage
    from langchain_core.tools import tool
    from app.workflows.rms_workflow.config import get_llm  (or wherever get_llm comes from)
    RAGController
    extract_actions, strip_actions
    Resource, Agent

Read reviewer_agent_node and approver_agent_node, list every
name they reference, and confirm each is imported. Restore any
that Task 6.1 accidentally removed.

DELIVERABLE
Show the corrected import block. Confirm reviewer_agent_node and
approver_agent_node have all their imports.
═══════════════════════════════════════════════════════════════════