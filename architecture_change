Let me solve both precisely — the timing report has the exact data needed, and the second issue is a scoping leak that needs to be traced, not guessed at.

---

## Issue 1: The 31-Second SQL Generator Call

Your instinct is right — this has nothing to do with the loading-status feature. The status feature only *displays* what's happening; it doesn't change execution time. Look at the actual numbers from your own log:

```
Intent LLM took 5.86 seconds
SQL Gen LLM took 31.05 seconds     ← the outlier
Chart LLM took 5.00 seconds
```

Three calls to the same LLM gateway, wildly different durations, and it's not proportional to prompt length in any way that would fully explain a 5x-6x jump. This points to one of three causes, and you can distinguish between them with a five-minute test rather than guessing.

**Most likely cause — this specific prompt structure is expensive for your gateway, not just "long."** Look at what's different about the SQL generator prompt versus the other two: it's the only one of the three that includes a large, structurally repetitive block — `schema_block`, which is the full table/column schema description built by `build_schema_description()`. Depending on how many tables and columns RMS_ANALYTICS currently has registered, that block could be genuinely large, and more importantly, it's the kind of dense, enumerated, rule-heavy content that tends to make LLMs generate more deliberated (slower) completions, especially combined with the 10 numbered "STRICT RULES" sitting right below it. The intent classifier prompt, by contrast, is short and asks for a two-word JSON answer. The chart prompt sends only the top 100 rows of already-filtered data, not the full schema.

**Second possible cause — gateway-side queueing/model routing, not prompt content at all.** If your internal LLM gateway load-balances across multiple backend instances or has per-request-type routing, it's possible the SQL generation call specifically is landing on a slower path for reasons unrelated to your prompt. You mentioned earlier in this project that your gateway has known quirks (stripping tool_calls, embedding API 401s) — it's plausible it has inconsistent latency characteristics too.

**Third, less likely but worth ruling out — output length.** If the LLM is asked to produce a long, deliberated single-block SQL query with no delimiter to stop early, and it's "reasoning" through the 10 rules before emitting the query (some models do visible or invisible step-by-step deliberation even without an explicit chain-of-thought instruction), that adds real wall-clock time independent of gateway load.

### How to actually diagnose this instead of guessing

Ask your AI to add one more timing checkpoint: log the **exact byte/token length of the schema_block** being sent for this specific query, alongside the existing timing print. Then run the same query type against two or three different collections with meaningfully different schema sizes (if you have RMS_ANALYTICS and HRMS_ANALYTICS both live, compare their generation times for a similarly-phrased question). If HRMS (smaller schema, fewer tables per your earlier schema registry work) generates noticeably faster than RMS for a comparable question, that confirms schema size as the driver, and the fix is straightforward: **trim the schema block to only the tables relevant to the question**, rather than always sending every table registered for that collection. You already have the infrastructure for this — a cheap pre-filter step (keyword match on table purpose descriptions, or even just always including the fact table and letting the LLM ask via a second turn if it needs more) would shrink the prompt substantially for most single-table questions.

If timing stays roughly flat regardless of schema size, the bottleneck is on the gateway side, not your prompt — and the fix becomes a conversation with whoever operates that internal gateway about routing/capacity for this endpoint, not something you can solve by editing prompts.

**One thing NOT to do:** don't strip down the 10 numbered rules to "shorten the prompt" as a first attempt. Those rules are there because you added them one at a time in response to real bugs you hit earlier in this build — the FACT/DIMENSION join rule specifically fixed a real data-integrity bug you found. Cutting rules to save latency risks reintroducing bugs you already paid to fix. Diagnose the actual cause first; don't trade correctness for speed on a guess.

---

## Issue 2: Status Messages Leaking Into MRR/RMS Workflow Chat

This is a real bug, and it's worth being precise about why it's happening, because the report's own description of the fix contains the explanation.

Look at what the engineering report says was done to fix Issue 1 from that same session:

> **Solution:** Implemented a fuzzy/substring matching algorithm: `next((k for k in ANALYTICS_NODE_STATUS.keys() if k in node_name), None)`

And separately, the report claims:

> **Workflow Isolation:** Guarded the status streaming with `if is_analytics:` to ensure standard RAG/NLP workflows weren't polluted with analytics-specific status updates.

**These two things directly contradict each other, and the contradiction is your bug.** If the `if is_analytics:` guard is correctly gating the entire status-emission block, then the fuzzy substring matcher inside that block should never even execute for RMS/MRR requests — it would never reach the point of checking node names at all, because the whole thing is supposedly skipped upstream. The fact that you're seeing status messages appear in MRR/RMS chat means **one of these two claims is false in the actual code**, not both true simultaneously as described.

The most likely explanation, given the ordering the report describes: the `is_analytics` guard was added correctly in `stream_chat_with_orchestrator`, but the fuzzy substring matcher was introduced *afterward*, as a fix for the "stuck on Starting..." bug — and it's plausible the substring matcher was added in a way that bypasses or sits outside the original guard, or the guard variable isn't correctly scoped/computed for every code path (`stream_chat_with_file` is a second, separate function per the report — if the guard was added to one function but the fuzzy-match fix was only tested against the other, you'd get exactly this asymmetric behavior).

There's also a more insidious possibility worth checking specifically: **the fuzzy substring match itself might be too loose and accidentally matching MRR/RMS node names that happen to share substrings with the analytics dictionary keys**, regardless of the `is_analytics` guard. For example, if `ANALYTICS_NODE_STATUS` has a key like `"agent"` mapped to "Agent is synthesizing response..." (this exact key appears in the earlier proposed mapping), and MRR's node names are things like `creator_agent_node` or `pl_pm_agent_node` (which you just renamed to that exact pattern), then `"agent" in "creator_agent_node"` is `True` — the substring match would fire for *any* workflow node containing the word "agent," not just analytics nodes. **This would explain the leak precisely and independently of whether the `is_analytics` guard exists**, because it's a separate, unguarded matching path if the fuzzy logic was added at a point in the code that isn't behind the original conditional.

### What to tell your AI, specifically

Don't accept "check why it's happening" as an open-ended investigation — give it the precise hypothesis to test, since you now have a strong lead:

```
Show me the EXACT current code for both stream_chat_with_orchestrator and 
stream_chat_with_file in chat_controller.py, specifically the section handling 
on_chain_start events and the ANALYTICS_NODE_STATUS lookup.

Answer these directly:
1. Is the fuzzy substring match (`next((k for k in ANALYTICS_NODE_STATUS.keys() 
   if k in node_name), None)`) located INSIDE the `if is_analytics:` block, 
   or does it execute unconditionally?
2. Does stream_chat_with_file have the same is_analytics guard as 
   stream_chat_with_orchestrator, or was the guard only added to one function?
3. List every key currently in ANALYTICS_NODE_STATUS. Check whether any key 
   (e.g. a short word like "agent") is a substring of any MRR or RMS node name 
   (e.g. creator_agent_node, pl_pm_agent_node) — if so, that's a false-positive 
   match independent of the is_analytics guard.
```

### The actual fix, regardless of which cause it turns out to be

Whichever of these is confirmed, the durable fix is the same: **the substring matching should never be allowed to run against non-analytics node names in the first place** — not "the guard usually works but the matcher is also loose." Two changes needed:

1. **Verify (don't just assume) the `is_analytics` guard wraps the entire status-emission block in both streaming functions**, checked before any node-name comparison happens at all — not just before the dictionary is consulted.
2. **Regardless of the guard, tighten the matching from substring to exact match**, or at minimum require a distinguishing prefix (e.g., only match keys that start with `analytics_` if that's how the renamed node names are structured, or use `node_name.startswith(k)` with fuller key names rather than bare substrings like `"agent"`). Fuzzy substring matching was a reasonable quick fix for the "LangGraph mangles node names" problem, but a bare word like `"agent"` is too permissive a pattern to safely use as a substring match across a codebase where you deliberately just renamed nodes to end in `_agent_node`. This is a direct consequence of the naming convention work you just did — the two features are interacting in a way neither was designed to anticipate.

This isn't breaking your workflow functionality today (you said so yourself — it's cosmetic, not functional), but I'd still get it fixed before your next adjustment, specifically because you're about to layer another workflow change on top, and cosmetic bugs that share a root cause with upcoming changes are exactly the kind of thing that becomes confusing to debug later if left in place. Cheap to fix now, more annoying to disentangle once it's tangled with whatever you do next.

Go ahead and tell me about the workflow adjustment you have queued up next.

issue 2 has been fixed and issue 1 i kept as 2nd priority because that needs proper debugging from my side and i will comeback to u regarding that.

Both locations updated (line 400 in stream_chat_with_orchestrator and line 811 in stream_chat_with_file).

Here's what changed:

Before (fuzzy substring — k in node_name):


matched_key = next(
    (k for k in ANALYTICS_NODE_STATUS.keys() if k in node_name), None
)
After (suffix/exact match — node_name.endswith(k) or node_name == k):


matched_key = next(
    (k for k in ANALYTICS_NODE_STATUS.keys() if node_name.endswith(k) or node_name == k), None
)
What this fixes:

k in node_name matched "sql_generator" inside "create_sql_generator_node" — any node with those 13 characters anywhere in its name would fire. False-positive territory.
node_name.endswith(k) only matches if the analytics node name is a suffix of the event name, which is what LangGraph produces (e.g. "MRR_graph|sql_generator" ends with "sql_generator").
The or node_name == k covers the exact-match case (e.g. "sql_generator" with no prefix).
Debug print removed since it was a temporary diagnostic.

* STILL I AM seeing Starting... for some queries in approvers part. ok since they were not effecting the functionlity i am not going to give that much time for them . if you feel any implementation gone wrong means tell me,
** coming to the problem that i told you

** current in mrr workflow when user submits a mrr via form of excel it will redirected to next node
# AI & Auto Verifier Pipeline
    graph_builder.add_edge("submit_mrr", "ai_verifier")
we are using it for saving a column with reasoning

"""
AI Verifier Agent (ReAct)
=========================
A silent background agent that reviews newly submitted MRRs using
the Bulletproof ReAct pattern.  It fetches data directly from the
database, analyses line items, quantities, and total cost, then
uses its Save_Verification tool to persist VERIFICATION_STATUS +
AI_VERIFICATION_REMARK directly into the DB via MRRDao.

The agent output is NEVER appended to state["messages"] — the frontend
SSE stream remains completely unaffected.
"""

from langchain_core.tools import Tool, tool

from app.core.llm import get_llm_non_streaming
from app.dao.mrr_dao import MRRDao
from app.workflows.material_workflow.agent_factory import (
    create_bulletproof_react_agent,
)


@tool
def Save_Verification(decision: str, reason: str) -> str:
    """
    Use this tool to finalize the AI verification result.
    Input should be JSON with 'decision' ('VERIFIED' or 'FLAG_REVIEW')
    and 'reason' (a brief explanation).
    """
    pass  # We provide the func wrapper at agent construction time


def run_ai_verifier(mrr_id: int, db) -> None:
    """
    Run the AI Verifier ReAct agent on the given MRR.
    Fetches data directly from the database for accuracy.
    """
    # ------------------------------------------------------------------
    # 1. Fetch real data from DB
    # ------------------------------------------------------------------
    breakdown = MRRDao.get_mrr_breakdown(db, mrr_id)
    total_cost = breakdown["total_cost"]
    line_items = breakdown["line_items"]

    items_summary = "\n".join(
        f"  {idx}. {item.get('item_name', 'Unknown')} | "
        f"Qty: {item.get('qty', 0):.2f} | "
        f"Unit Cost: {item.get('cost', 0):,.2f} | "
        f"Subtotal (Qty * Unit Cost): {item.get('subtotal', 0):,.2f} | "
        f"UOM: {item.get('uom', 'EA')}"
        for idx, item in enumerate(line_items, 1)
    )

    # ------------------------------------------------------------------
    # 2. Define the Save_Verification tool (wraps the @tool signature)
    # ------------------------------------------------------------------
    def _save_verification(decision: str, reason: str) -> str:
        decision_upper = decision.strip().upper()
        final_decision = "FLAG_REVIEW" if "VERIFIED" not in decision_upper else "VERIFIED"
        MRRDao.update_ai_verification(db, mrr_id, final_decision, reason)
        return f"Verification saved: decision={final_decision}, reason={reason}"

    tools = [
        Tool(
            name="Save_Verification",
            func=_save_verification,
            description=(
                "Use this tool to finalize the AI verification result. "
                "Input should be a JSON string with keys 'decision' "
                "('VERIFIED' or 'FLAG_REVIEW') and 'reason' (full explanation)."
            ),
        )
    ]

    # ------------------------------------------------------------------
    # 3. Define the ReAct prompt
    # ------------------------------------------------------------------
    prefix = (
        "You are the MRR Auditor Agent. "
        "Evaluate the following Material Requisition (MRR) cost against the "
        "2000 INR approval-limit threshold.\n\n"
        "If Total Cost > 2000, your decision MUST be FLAG_REVIEW.\n"
        "If Total Cost <= 2000, your decision MUST be VERIFIED.\n\n"
        "After determining the decision, you MUST call your Save_Verification "
        "tool — do NOT provide a final answer without saving.\n\n"
        "Format strictly:\n"
        "Thought: Do I need to use a tool? Yes\n"
        "Action: the action to take\n"
        "Action Input: the input to the action\n"
        "Observation: the result\n"
        "Thought: I now know the final decision.\n"
        "Final Answer: [Your summary]\n\n"
        "Items:\n"
        "{items_summary}\n\n"
        "Total Cost: {total_cost}\n"
        "Approval Limit: 2000"
    )

    suffix = "Begin!\nQuestion: {input}\nThought: {agent_scratchpad}"

    # ------------------------------------------------------------------
    # 4. Build and execute the bulletproof agent
    # ------------------------------------------------------------------
    agent = create_bulletproof_react_agent(
        tools=tools, prefix=prefix, suffix=suffix
    )

    try:
        response = agent.invoke({
            "input": "Verify this MRR.",
            "items_summary": items_summary,
            "total_cost": f"{total_cost:,.2f}",
        })
    except Exception as e:
        # Fallback: always save something deterministic
        fallback_reason = f"AI agent encountered an error during analysis ({e})."
        MRRDao.update_ai_verification(db, mrr_id, "FLAG_REVIEW", fallback_reason)
** now what is happening is currently after clicking on submit button this message is coming ⏳ Analyzing pending requests...
** in the backend this is happening 
Question: Verify this MRR.
Thought: Do I need to use a tool? Yes
Action: Save_Verification
Action Input: {"decision": "VERIFIED", "reason": "The total cost of 208.00 INR is within the 2000 INR approval limit threshold."}[DIAG-MRR] auto_verifier_node entered. new_requisition_id=155

** it is not giving the final answer at all.

** actually what has to happening we will never showing any reasoning to user when he submits that particular node is for just inserting that remarks column only. so it will disturb the UX completely for that only we are using 
a small reasoning for user to just give a friendly message thats it.

# Update state for auto-verifier
        confirmation = add_reasoning(
            f"Your MRR **{header.get('mrr_no', f'MRR/{next_header_id}')}** has been submitted.\n"
            f"Total cost: \u20b9{header.get('total_cost', 0):,.2f}\n\n"
            f"Running verification...",
            context=f"MRR {header.get('mrr_no', str(next_header_id))} submitted by {header.get('requestor_id', '')}; "
                    f"total cost \u20b9{header.get('total_cost', 0):,.2f}; awaiting auto-verification."
        )

def add_reasoning(original_message: str, context: str) -> str:
    """
    Optionally append a short LLM-generated reasoning sentence to a
    deterministic success message. ALWAYS returns a string. If the
    feature is disabled or the LLM call fails, returns the original
    message unchanged.

    Args:
        original_message: the deterministic procedural output. This
            is what the user MUST see; the LLM only adds context.
        context: a brief description of what the procedural step did,
            used to prompt the LLM. Plain text, no formatting needed.

    Returns:
        original_message [+ "\\n\\n" + reasoning sentence] on success.
        original_message unchanged on disable/failure.
    """
    if not getattr(settings, "ENABLE_REASONING_ADDON", False):
        return original_message

    try:
        from app.core.llm import get_llm_non_streaming
        llm = get_llm_non_streaming()

        prompt = (
            "You are explaining a workflow action briefly to a user.\n"
            "The system has ALREADY completed the action and produces "
            "the confirmation below. Your job is to add ONE short, "
            "friendly sentence (max 25 words) that explains what just "
            "happened or what to expect next. Do NOT repeat the "
            "confirmation. Do NOT add bullet points, markdown, or "
            "headers. Plain prose, one sentence.\n\n"
            f"Action context: {context}\n\n"
            f"System confirmation:\n{original_message}\n\n"
            "Your one-sentence addition:"
        )

        # Use synchronous invoke with tags so the controller can
        # filter out these internal events from the streaming output.
        response = llm.invoke(
            prompt,
            config={"tags": ["reasoning_addon"], "run_name": "reasoning_addon"},
        )
        reasoning = getattr(response, "content", "") or ""
        reasoning = reasoning.strip()

        if not reasoning:
            return original_message

        # Strip any leading/trailing quotes, code fences, list bullets
        for prefix in ("- ", "* ", "> ", "\"", "'"):
            if reasoning.startswith(prefix):
                reasoning = reasoning[len(prefix):].strip()
        for suffix in ("\"", "'"):
            if reasoning.endswith(suffix):
                reasoning = reasoning[:-1].strip()

        return f"{original_message}\n\n{reasoning}"

    except Exception as e:
        # Silent fallback -- log to stderr for diagnostics but never
        # propagate the failure to the user.
        try:
            print(f"[REASONING-ADDON] Failed; returning original. err={e}", flush=True)
        except Exception:
            pass
        return original_message

** now instead of this friendly message that REact part is blocking the frontend it seems to be which is causing terrible UX after creation.
========================

there is one more place we are using react which is for approver login i tried one test case for that 


 show pl approvals

> Entering new AgentExecutor chain...


Thought: I need to retrieve the current list of pending MRR approvals to display to the user.
Action: List_Pending_Approvals
Action Input:
Observation: Pending MRRs:
- ID: 154 | True Cost: 1,079.00 | MRR: MRR/154/26-27 | Remarks: AI agent encountered an error during analysis (run_ai_verifier.<locals>._save_verification() missing 1 required positional argument: 'reason').
- ID: 150 | True Cost: 8,429.61 | MRR: DPE/MRR/48/26-27 | Remarks: AI agent encountered an error during analysis (run_ai_verifier.<locals>._save_verification() missing 1 required positional argument: 'reason').
- ID: 148 | True Cost: 9,654.61 | MRR: DPE/MRR/46/26-27 | Remarks: AI agent encountered an error during analysis (run_ai_verifier.<locals>._save_verification() missing 1 required positional argument: 'reason').
- ID: 140 | True Cost: 1,630.86 | MRR: MRR/140/26-27 | Remarks: AI agent encountered an error during analysis (run_ai_verifier.<locals>._save_verification() missing 1 required positional argument: 'reason').
- ID: 138 | True Cost: 46.20 | MRR: MRR/138/26-27 | Remarks: AI agent encountered an error during analysis (run_ai_verifier.<locals>._save_verification() missing 1 required positional argument: 'reason').
- ID: 137 | True Cost: 996,556.00 | MRR: MRR/137/26-27 | Remarks: AI agent encountered an error during analysis (run_ai_verifier.<locals>._save_verification() missing 1 required positional argument: 'reason').
- ID: 105 | True Cost: 250.00 | MRR: MRR/105/26-27 | Remarks: None
- ID: 53 | True Cost: 153.00 | MRR: MRR/53/26-27 | Remarks: None
- ID: 51 | True Cost: 2.30 | MRR: MRR/51/26-27 | Remarks: None
- ID: 48 | True Cost: 24.00 | MRR: MRR/48/26-27 | Remarks: None
- ID: 47 | True Cost: 27.50 | MRR: MRR/47/26-27 | Remarks: None
- ID: 46 | True Cost: 5.80 | MRR: MRR/46/26-27 | Remarks: None
- ID: 42 | True Cost: 146.80 | MRR: MRR/42/26-27 | Remarks: None
Thought:

Final Answer: I have retrieved the list of pending MRR approvals for your role. You will find the details displayed in the UI table for further action.

** here somehow it is giving the reasoning in frotnend and also that form table is coming.

** i am provinding you this code also FYR.

 tools = [
        Tool(
            name="List_Pending_Approvals",
            func=fetch_pending,
            description="Use this tool to fetch the list of pending MRRs for your role. Input: empty string.",
        ),
        Tool(
            name="Approve_MRR_By_ID",
            func=approve_one,
            description="Use this tool to approve a specific MRR. Input should be a JSON string with 'mrr_id' (integer) and optionally 'reason' (string).",
        ),
        Tool(
            name="Reject_MRR_By_ID",
            func=reject_one,
            description="Use this tool to reject a specific MRR. Input should be a JSON string with 'mrr_id' (integer) and optionally 'reason' (string).",
        ),
        Tool(
            name="Approve_All",
            func=approve_all,
            description="Use this tool to approve all pending MRRs for your role. Input: empty string.",
        ),
    ]
    return tools


def run_approver_react_agent(user_input: str, emp_no: str, persona: str) -> str:
    """
    Run the ReAct agent and return a cleaned-up Final Answer.
    """
    tools = _build_approver_tools(emp_no, persona)

    prefix = (
        "You are the MRR Approver Agent, acting on behalf of the "
        f"{persona} role.\n\n"
        "Your role is to help the approver manage their pending MRR requests.\n\n"
        "Rules:\n"
        "1. Always start by using List_Pending_Approvals to see what's available.\n"
        "2. If the user asks to approve/reject a specific MRR, use the respective tool.\n"
        "3. If the user asks to approve all, use Approve_All.\n"
        "4. Always maintain a polite, conversational tone in your Final Answer.\n"
        "5. Summarize the outcome in 1-2 sentences — do NOT list individual items\n"
        "   (the UI displays the table).\n\n"
        "Format strictly:\n"
        "Thought: I need to determine the next step\n"
        "Action: ActionName\n"
        "Action Input: the action input\n"
        "Observation: the tool result\n"
        "Final Answer: Your conversational response."
    )

    agent = create_bulletproof_react_agent(
        tools=tools,
        prefix=prefix,
        suffix="Begin!\nQuestion: {input}\nThought: {agent_scratchpad}",
    )

    try:
        response = agent.invoke({"input": user_input})
        return response.get("output", "No response from the approver agent.")
    except Exception as e:
        return f"Error from approver agent: {e}"


====================================

** now what i want since i didnt get any approval from management to remove this REact and follow bind_tools and some other structured approach which u suggested earlier. so i cannot remove this.
** but today we have a small demo so i dont want to change that much for today but somehow i want to that veriifer agent ⏳ Analyzing pending requests.. meassage to be gone . i want that friendly reasoning should come because any ways
this REact reasoning is not required for users to get display it is just db insertion as i said.

**how to solve this 
