Here is your exact, sequenced execution plan based directly on Management’s strict orders. Do not do all of these at once. Do them one by one, and run the specified test before moving to the next step.
Step 1: Delete run_approver_summary

    Open app/workflows/material_workflow/approver_react_agent.py.

    Scroll to around line 268 and completely delete the run_approver_summary function.

    TEST: Run an approval flow (chat-based approve/reject) to ensure the approver agent still works perfectly.

Step 2: Delete the Dead Functions in nodes.py

    Open app/workflows/material_workflow/nodes.py.

    Delete the first _parse_approval_command (around line 119).

    Delete the duplicate _parse_approval_command (around line 988).

    Delete the duplicate _get_last_user_message (around line 978). (CRITICAL: Leave the active one at line 144 alone!)

    TEST: Run both the Form Submit flow and the Approval flow to ensure no side effects occurred.

Step 3: Delete mrr_tools.py

    Completely delete the file app/workflows/material_workflow/mrr_tools.py.

    TEST: Run your full MRR regression checklist (Creator chat/Excel, Approver chat/Excel, Form submit, AI verifier).

Step 4: Delete creator_agent.py

    Open app/workflows/material_workflow/nodes.py one last time.

    Go to line 49 and delete the dead import: from app.workflows.material_workflow.creator_agent import run_creator_agent.

    Completely delete the file app/workflows/material_workflow/creator_agent.py.

    TEST: Run your full MRR regression checklist one final time.


===========================================

report results by AI

Workflow Cleanup Status & Backlog Report

Phase 1: Dead Code Removal (COMPLETED)

    Action: Conducted a full-repository static and dynamic string analysis to verify orphaned code. Safely deleted mrr_tools.py, creator_agent.py, and multiple dead functions/imports from nodes.py and approver_react_agent.py.

    Result: Clean bill of health. Full regression testing (Creator flow, Approver flow, Auto-Verifier ReAct agent) passed with zero side effects.

Phase 2: MRR DAO SQL Migration (LOGGED TO BACKLOG)

    Context: Scans identified raw SQL execution (e.g., INSERT INTO mrr_matrequisitionheader, UPDATE) leaking into workflow nodes (specifically nodes.py, auto_verifier_node, and approver_react_agent.py).

    Risk Assessment: High. Unlike Phase 1, migrating these queries to mrr_dao.py requires rewriting active, live-production database write paths. Any subtle mismatch in transaction scoping, parameter binding, or error handling will directly break the requisition creation and approval flows.

    Recommendation: Do not execute as a standalone "cleanup" task. This migration has been logged to the backlog. It should be scheduled as a dedicated architectural refactor with its own isolated testing cycle, ideally piggybacking on the next feature request that requires modifying the MRR creation logic anyway.

==============================================

i think we can start phase 2 now . give me a very good plan to complete this cleanly
